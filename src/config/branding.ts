// Centralized white-label branding for the wallet.
// Import `BRAND` anywhere a name/ticker is displayed instead of hardcoding strings.
export const BRAND = {
    name: "R Wallet",
    shortName: "R Wallet",
    ticker: "REV",
    subunit: 8,
    packageName: "r-wallet",
} as const;
